import { defineStore } from 'pinia';

const defaultState = {
 
};

export default defineStore('pdf_signature_flag', {
  state: () => ({ ...defaultState }),
  getters: {

  },
  actions: {
    
  },
});